# Set path for collection NGINX status
location = /nginx/stub/status {
  access_log off;

  include edge/edge-ip-restrictions;
  # include edge/edgecast-ip-restrictions;
  # include edge/cloudflare-ip-restrictions;
  deny all;

  include edge/headers-nocache.conf;

  stub_status;
}
